const os = require ('os')

console.log(os.cpus()) // Quantas CPU's tem na maquina

console.log(os.freemem()) // Memoria RAM

console.log(os.homedir()) // Qual diretório principal da máquina

console.log(os.type()) // Qual sistema operacional